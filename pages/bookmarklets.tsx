import BookmarkAdder from '../components/Home/BookmarkAdder'

type Props = {}

const bookmarklets = (props: Props) => {
  return (
    <div>
      <h1>Bookmarklets</h1>

      <BookmarkAdder />
    </div>
  )
}

export default bookmarklets
